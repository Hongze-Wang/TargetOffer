如果现在给你一个很大的字符串集合，比如包含 1 万条记录，如何通过编程量化分析这组字符串集合是否比较适合用 Trie 树解决呢？也就是如何统计字符串的字符集大小，以及前缀重合的程度呢？

依次读取每一个字符构建Trie树，每一层的结点用一个链表串起来。
求某一长度的前缀重合，就在对应树层级上遍历该层链表，求链表长度。
用散列表存储每一个结点(key=字符，value=字符出现的次数)，即遍历所有树层级的链表，存入散列表，最后散列表包含的元素个数，就代表字符集的大小。
链表长度除以字符集大小，值越小前缀重合率越高。
