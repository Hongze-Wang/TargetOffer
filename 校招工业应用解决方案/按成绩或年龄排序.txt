根据年龄给100万用户排序/根据成绩给50万学生排序。

可以说是桶排序，但究其实质是计数排序：
假设年龄最小为1岁，最大不超过120岁。遍历这100万用户，根据年龄将其划分到120个桶里，然后再顺序遍历这120个桶中的元素，这样就得到了按照年龄排序的100万用户数据。

这其实是计数排序的思想，只不过如果桶排序桶的范围粒度是1，而不是类似0~1000这种区间范围，其实就是计数排序的思想了。
从这一点上看，计数排序可以看做是特殊的桶排序，每个桶仅对应一个值而不是一个范围。